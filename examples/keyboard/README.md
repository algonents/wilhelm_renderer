# Keyboard

Demonstrates keyboard input handling. Key presses, releases, and repeats are logged to the console with modifier key detection (Ctrl, Shift, Alt).

```shell
cd examples/keyboard && cargo run
```
