# Z-Order

Demonstrates z-ordering of shapes. Overlapping shapes are rendered according to their z-order value, independent of insertion order.

<img src="../../images/z_order.png" width="50%">

```shell
cd examples/z_order && cargo run
```
