# Window

Basic window creation with event callbacks. Demonstrates resize, mouse cursor position, and scroll events logged to the console. Also reports the detected platform (X11, Wayland, macOS, Windows).

```shell
cd examples/window && cargo run
```
